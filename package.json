{
  "name": "c-ssl",
  "version": "0.0.0",
  "private": true,
  "description": "SSL 证书签发与销售平台",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22 <23",
    "pnpm": ">=10"
  },
  "scripts": {
    "env:check": "make env-check",
    "spec:check": "make spec-check",
    "gen": "make gen",
    "gen:check": "make gen-check",
    "typecheck": "make typecheck",
    "lint": "pnpm -r --if-present run lint",
    "build": "pnpm -r --if-present run build"
  },
  "devDependencies": {
    "openapi-typescript": "^7.10.1",
    "typescript": "^5.9.3"
  }
}
